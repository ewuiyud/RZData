<Window x:Class="RZData.Views.RevitDataCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RZData.Views"
             mc:Ignorable="d" 
             Title="睿住数据"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid>
        <StackPanel >
            <Button Content="加载Excel数据" Command="{Binding LoadDataFromExcelCommand}" Width="100" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>
            <Button Content="检查族信息" Command="{Binding CheckFamiliesCommand}" Width="100" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>
            <TabControl>
                <TabItem Header="族匹配校验" Margin="0">
                    <TreeView ItemsSource="{Binding Elements.Families}" Margin="20" Height="330">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding FamilyTypes}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" Width="200"/>
                                </StackPanel>
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding FamilyExtends}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" Width="200"/>
                                        </StackPanel>
                                        <HierarchicalDataTemplate.ItemTemplate>
                                            <HierarchicalDataTemplate ItemsSource="{Binding Parameters}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" Width="200"/>
                                                    <TextBlock Text="{Binding Value}" Width="200"/>
                                                </StackPanel>
                                            </HierarchicalDataTemplate>
                                        </HierarchicalDataTemplate.ItemTemplate>
                                    </HierarchicalDataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Grid>
</Window>
