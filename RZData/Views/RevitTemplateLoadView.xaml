<Window x:Class="RZData.Views.RevitTemplateLoadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RZData.Views"
             mc:Ignorable="d" 
             Title="模板载入"
             MaxHeight="270" MinHeight="270" MaxWidth="550" MinWidth="550">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/RZData;component/Styles/Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="选中文件：" Margin="30,50,10,30" FontFamily="微软雅黑 light" FontSize="15" VerticalAlignment="Center" />
            <TextBox Width="230" Height="30" IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding LoadFileName}"
                     Margin="0,50,30,30" FontFamily="微软雅黑 light" FontSize="15" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="1"  Orientation="Horizontal">
            <TextBlock Text="当前模板：" Margin="30,10,10,30" FontFamily="微软雅黑 light" FontSize="15" VerticalAlignment="Center" />
            <TextBox Width="230" Height="30" IsReadOnly="True" Text="{Binding CurrentFileName}"
                     Margin="0,20,0,40" VerticalContentAlignment="Center"  FontFamily="微软雅黑 light" FontSize="15" VerticalAlignment="Center"/>
        </StackPanel>
        <Button Content="选择文件"   Grid.Column="1"
                    Command="{Binding LoadDataFromExcelCommand}" Width="120" FontSize="15" Height="35" HorizontalAlignment="Left" Margin="10,50,30,30"/>
        <Button Grid.Row="1" Grid.Column="1" Content="确定" FontSize="15" 
                Command="{Binding OKCommand}" Width="120" Height="35" HorizontalAlignment="Right" Margin="10,15,30,15"/>
    </Grid>
</Window>
