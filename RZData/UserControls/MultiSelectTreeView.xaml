<UserControl x:Class="RZData.UserControls.MultiSelectTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RZData.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="TreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="FontFamily" Value="微软雅黑 Light"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <EventSetter Event="Loaded" Handler="TreeViewItem_Loaded"/>
        </Style>
    </UserControl.Resources>
    <Border CornerRadius="3" Background="White" Padding="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="1" Color="Gray"/>
        </Border.Effect>
        <Grid>
            <TreeView x:Name="multiSelectTreeView" BorderThickness="0"
                  ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItemChanged="TreeView_SelectedItemChanged"
                  ItemContainerStyle="{StaticResource TreeViewItemStyle}"
                  FontSize="14" MouseDoubleClick="MultiSelectTreeView_MouseDoubleClick">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox x:Name="nodeCheckBox"
                                  VerticalAlignment="Center"
                                  Margin="2"
                                  IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                  Click="CheckBox_Click"/>
                            <TextBlock Text="{Binding Name}" Width="auto"/>
                        </StackPanel>
                        <HierarchicalDataTemplate.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox x:Name="nodeCheckBox"
                                          VerticalAlignment="Center"
                                          Margin="2"
                                          IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                          Click="CheckBox_Click"/>
                                    <TextBlock Text="{Binding Name}" Width="auto" Margin="2 0 0 0"/>
                                </StackPanel>
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox x:Name="nodeCheckBox"
                                                  VerticalAlignment="Center"
                                                  Margin="2"
                                                  IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                  Click="CheckBox_Click"/>
                                            <TextBlock Text="{Binding Name}" Width="auto" Margin="2 0 0 0"/>
                                        </StackPanel>
                                        <HierarchicalDataTemplate.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="nodeCheckBox"
                                                          VerticalAlignment="Center"
                                                          Margin="2"
                                                          IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                          Click="CheckBox_Click"/>
                                                    <TextBlock Text="{Binding Id}" Width="auto" Margin="2 0 0 0"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </HierarchicalDataTemplate.ItemTemplate>
                                    </HierarchicalDataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Grid>
    </Border>
</UserControl>